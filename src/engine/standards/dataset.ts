import raw from "./data/k_ela_best.json";
import type { BestStandardRow } from "./types";

/** Kindergarten ELA B.E.S.T. dataset (public). */
export const K_ELA_BEST_DATASET = raw as BestStandardRow[];
